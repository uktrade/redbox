# Minimum environment variables for the retrieval eval harness.
# These allow redbox.* modules to import without a full Django/production stack.
# Real values (AWS keys, OpenSearch URL, etc.) come from your shell environment or .env at the repo root.
ENVIRONMENT=LOCAL
ENABLE_METADATA_EXTRACTION=true
